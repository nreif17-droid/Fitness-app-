# Sleep Domain

Not yet designed in depth. Placeholder scope:

- Logging: manual at minimum (hours + a 1-10 quality self-report).
  Wearable integration (via Base44's integration connectors, if available —
  confirm against current docs) is a natural enhancement, not a dependency.
- This data feeds directly into `ReadinessCheckIn` and the coach's
  autoregulation logic — the "sleep 7-9h" non-negotiable and the "two
  consecutive nights of poor sleep" deload trigger are already codified in
  `domains/training/knowledge-base/00-framework.md`.
