# Goals Domain

Not yet designed in depth. Placeholder scope:

- Goals get captured at onboarding and re-evaluated at program block
  boundaries (roughly every 12 weeks, matching the training library's
  cadence — see `09-usage-guide.md`'s "sequencing across a year" section).
- A goal should be specific enough to drive template selection (a target
  metric and a rough timeline), not just an aspiration — the intake AI job
  in `domains/ai-coach/orchestration.md` is responsible for getting a vague
  goal to that level of specificity before a program gets assigned.
